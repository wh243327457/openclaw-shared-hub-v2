# 每日复盘 2026-09-07

> 数据来源：Claude Code 0 条 | Codex 3728 条 | Chrome 441 次（公开 314 / 内网 127）
> 说明：Codex 原始采集包含 09-03 至 09-06 的历史回放；正文仅使用北京时间 09-07 的 1223 条活动，并过滤系统注入、内部续传与重复内容。
> 生成方式：自动摘要模型不可用，由 Hermes 基于本地 raw 数据降级生成。

---

## 1. 今日概览

今天的工作集中在两条业务主线：一是持续收口 CD10910 次品/极速换货申请、支付、审核、库存与工单闭环；二是将 10814“人工设置折后价”从过度设计收敛为最小兼容方案。整体取得了多项代码提交和真实链路证据，但 CD10910 的退款成功后库存释放仍有待修复，完整端到端验收尚未收口。

## 2. 主要工作

### 2.1 CD10910：极速换货旧分支修复与 PRD 9.4 落地

- 修复 App 未传 SKU 时申请创建失败的问题：由服务端依据订单明细回填标准 SKU，并增加回归测试；本地提交 `c63cf5d87c`。
- PRD 9.4 增量完成：首次审核超时 50 分钟、补资料重提 30 分钟、同 `rec_id + SKU` 整组库存判断，以及 OS/newos 的极速类型与支付状态展示。
- Go、PHP、前端构建及 OpenSpec 严格校验通过；任务进度记录为 45/58，剩余为联调、发布与完整验收项。
- 四仓分别提交本轮增量：`service-order 79884c6f`、`yishouapp a64ee008`、`yishouos 83a820a7`、`newos a6519327`。

### 2.2 CD10910：脚本归属、短信与过度改动收口

- 梳理三项目 16 个运行项及 DDL，明确本次只新增库存预占表，不改申请单、工单等既有大表字段。
- 将调度入口收敛到既有 PHP 脚本体系，Go 保留原子业务与事务能力；撤回与本需求无关的普通换货迁移和额外批处理。
- 审核短信改为 PHP 调用 Go 成功后直接发送，去掉专用队列与 Redis 去重；已通过 WSL `php72` 容器真实发送并由接收端确认收到。
- 三仓提交分别为 `34b909ab14`、`503b9579b3`、`5a2ad3d`；随后又按 stable 清理 13 个无关改动并提交 `8af18e9d9e`、`32c0ce3c59`、`d53219bf`。

### 2.3 CD10910：真实申请链路、库存回补与 App/OS 展示

- 真实申请 `3210234 / 2609073421084` 完成余额支付 102 元、人工审核通过并生成唯一正式工单 `101538903`；申请、支付、工单及预占关联一致。
- 工单结束后暴露正式工单库存回补缺口；已实现“只恢复同一 App 业务日内未发货部分、跨业务日不回补、重复执行不重复恢复”，14 个定向测试及数据库条件验证通过。
- 真实 ERP 查询返回 503，导致该单库存回补未完成，已登记为人工复测；相关 yishouapp 代码提交 `43328b640e`。
- 修复 App 售后列表遗漏申请状态 9/10、待支付展示和 `rel_code` 单申请兼容；OS 自动分单纳入待支付待审核状态 9，状态 10 不进入审核队列。

### 2.4 CD10910：从 stable 重建最小兼容实现

- 三项目从最新 `origin/stable` 建立 `feat-11061-次品换货迭代&换货审核逻辑-902`，重新整理 40 项任务；旧分支只作参考，不再整体移植。
- 完成普通/极速申请的独立服务端分流：免审直建保持 stable，非免审用户先生成申请，审核通过后再建正式工单。
- 库存方案收敛为 yishouapp 创建前预占 Redis、Go 事务内绑定；申请转工单继承预占，不重复扣减；每日和实时同步统一扣除有效预占。
- 真实 App 链路验证：申请 `2609073421088` 创建后库存 `483 → 482`，取消后恢复 `483`，重复取消不重复回补。
- 申请 `3210280` 已完成余额支付 101 元并进入待审核；本地 service-order、yishou-go、Modi 均已启动并打通权限链路。
- 当前阻塞：已付申请终态结算夹具在“退款成功后释放库存”阶段仍失败，尚未执行真实退款，不能标记为通过。

### 2.5 10814：人工设置折后价最小方案与验证

- 将方案收敛为：复用原 `discount_type` 增加类型 3，并在两张已有配置表各增加一个 `fixed_price`；不新建表，不给订单和历史链路增加额外字段。
- DDL 已由人工添加到测试库；真实货号 `1379900005` 验证固定价 21.01/29.00 均原值生效，展示折扣仅用于展示，不反算售价，测试数据随后回滚。
- OS/API 首轮代码已推送；随后再收窄 3 个文件，保持旧类型 ES 折扣来源不变，并取消固定价商品的额外抓款过滤，推送 `OS 75422036f3`、`API 90029f8bbd`。
- 货号 `1379700003` 已通过选品规则按 29.00 元上架，数据库与上架日志确认，用户侧验证通过。
- 已整理前端联调说明：不新增接口，只扩展原保存/回显参数；测试环境自动调度是否正常消费仍待验证。

## 3. 值得沉淀

1. **先基线、后差异、再最小移植**：需要回归验证的模块不等于必须修改的模块，不能把潜在风险提前扩成开发范围。
2. **申请态与工单态必须分开建模**：待支付申请、已审核待支付、正式工单不能复用同一套列表过滤和支付校验。
3. **库存验收必须看完整状态链**：预占表、Redis 实际值、支付/审核状态和正式工单关联要一起核对，单看某一张表不足以证明成功。
4. **同步任务要扣除有效预占**：外部库存刷新后仍需减去本地有效预占，且跨业务日只结束记录、不回补旧日 Redis。
5. **固定金额直接决定售价**：折扣只由固定价除以原价计算用于展示，不能再用展示折扣反算售价。

## 4. 明日线索

- 继续定位并修复“退款成功后释放库存”失败条件，补齐独立事务夹具后再跑真实申请退款与库存回补。
- 对 stable 重建分支继续按 40 项任务逐项推进，保持开关关闭，避免未完成链路进入测试环境主流量。
- 完成申请 `3210280` 的本地 OS 审核建单、重复审核和付款/预占/工单三方关联验证。
- 补测 10814 的 OS 页面保存、自动抓款/上架调度和 App 实际展示，确认不是依赖人工定向执行。
- 清点尚未推送的 CD10910 提交与部署版本，确保测试环境代码、数据库和服务配置基线一致。

---

## 5. 今日访问概览（Chrome）

| 次数 | 域名 | 主要用途 |
|---:|---|---|
| 90 | `osweb-feat-11061.yishou.pro` | OS 申请、审核与工单页面联调 |
| 77 | `prd-upload-pub.yishouapp.com` | CD10910 PRD 核对 |
| 69 | `get-token.vip` | AI/工具服务使用记录 |
| 44 | `sub.aikangs.com` | AI 服务管理与使用记录 |
| 28 | `gitlab.yishou.pro` | 代码仓库与合并请求 |
| 18 | `keycrop.net` | 数字商品与订阅平台 |
| 15 | `yearning.yishou.com` | 数据库只读查询 |
| 12 | `zentao.yishou.com:8088` | 需求与任务管理 |
| 9 | `yb7ao262ru.feishu.cn` | 飞书协作 |
| 6 | `erpweb-stable.yishou.pro` | ERP 业务核验 |

**访问侧印证：**工作重心与 Codex 记录一致，主要集中在 CD10910 OS/PRD/数据库联调、GitLab 代码处理，以及 10814 选品规则验证。

---

*数据来源：Claude Code 0 条 | Codex 3728 条 | Chrome 441 次*
