
## Docker OpenClaw 复用 shared hub smoke test 收口（2026-04-25T13:08:56+08:00）

- 计划文档：`shared/docs/plans/2026/04/2026-04-25-docker-openclaw-shared-hub.md`
- 项目状态：`shared/curated/memory/projects/shared-hub-v2.md`
- 结论：容器侧可读取 shared v2 入口；OpenClaw 可写目录限定到 `inbox/openclaw/daily`、`runtime/openclaw`、`compat/daily`；`curated/memory` 保持只读。
- 实际卡点：root 运行时若不设置 `HOME=/home/node`，OpenClaw 会把 auth store 定位到 `/root/.openclaw/.../auth-profiles.json`，触发 `No API key found for provider openai`。
- 推荐策略：短期 smoke test 使用 `--user 0:0 -e HOME=/home/node`；长期改宿主 ACL/owner，让容器以 node 用户运行。


### 2026-04-25T13:50:41+08:00 OpenClaw MiniMax 配置修正

- 审计发现：OpenClaw MiniMax provider 已存在，但 baseUrl 使用 `https://api.minimax.chat/v1`，Hermes env 中实际可用的是 `MINIMAX_CN_API_KEY`。
- 执行：已备份并更新 `openclaw.json` 与 `agents/main/agent/models.json`，改为 `https://api.minimaxi.com/v1` + `${MINIMAX_CN_API_KEY}` 占位。
- 安全：未向 shared 写入明文 secret。
- 验证：配置 JSON 解析通过，MiniMax 两个模型存在，shared `verify_bridge.py` 返回 ok。
## Docker OpenClaw / MiniMax / shared bridge 收口（2026-04-25T16:18:49+08:00）

- 执行结论：Docker OpenClaw 当前容器名已收口为 `openclaw`，镜像 tag 已补 `openclaw:latest`。
- 运行状态：容器 `openclaw` 使用镜像 `ghcr.io/openclaw/openclaw:latest`，状态健康，端口映射 `18790 -> 18789`。
- MiniMax smoke：`openclaw infer model run --local --model minimax/MiniMax-M2.7 ... --json` 返回 `ok: true`，provider 为 `minimax`，model 为 `MiniMax-M2.7`。
- shared 验证：`scripts/verify_bridge.py` 返回 `ok: true`；errors / warnings 为空；governance、future_agent_readiness、shared_skills_manifest、runtime_retention 均通过。
- 安全说明：未向 shared 写入明文 secret。
- 后续约束：仍建议长期修正宿主目录权限，让容器以 node 用户运行；不要把整个 shared 作为长期 RW 挂载。

