version: 1
name: shared-hub-v3-manifest-schema
required:
  - version
  - name
  - layout
  - root_model
  - roots
  - kernel
constraints:
  version_equals: 3
  name_equals: shared-hub-v3
  layout_equals: protocol-first
  root_model_equals: minimal-kernel
