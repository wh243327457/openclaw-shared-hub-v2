version: 3
name: shared-hub-v3
seed: true
status: stage3
layout: protocol-first
root_model: minimal-kernel
roots:
  host: /home/vany/openclaw-data/.openclaw/shared.v3
  container: /home/node/.openclaw/shared.v3
  note: stage3 seed is incubated inside next/shared-hub-v3/ in the current v2 staging repo
kernel:
  agents: agents
  registry: registry
  truth: truth
  sandbox: sandbox
contracts:
  truth_source: truth/
  write_entry: sandbox/
  compatibility: adapters-not-storage
  capability_registry: registry/capabilities/manifest.yaml
  agent_schema: schema/agent.schema.yaml
  promote_protocol: protocol/promote-protocol.md
  promote_executor: tools/promote_executor.py
  forbidden_root_dirs:
    - compat
    - memory
    - skills
policies:
  orchestrator: hermes
  direct_truth_write_default: false
  plaintext_secrets: forbidden
