# Shared hub v2 git safety ignore
# Runtime / agent-local artifacts
runtime/
**/.dreams/
**/cache/
**/.cache/
**/tmp/
**/temp/
**/__pycache__/
*.pyc
*.pyo

# Raw / bulk histories should not grow in Git by default.
# Existing tracked files are handled in later slimming phases; these rules
# prevent new dreaming/.dreams bulk from entering the review surface.
inbox/**/daily/dreaming/
compat/daily/dreaming/
inbox/**/daily/.dreams/
compat/daily/.dreams/

# Logs and generated diagnostics
*.log
logs/
*.trace
*.dump

# Environment and credentials - never commit plaintext secrets
.env
.env.*
*.key
*.pem
*.token
*.crt
*.p12
*.pfx
*secret*
*secrets*
*credential*
*credentials*
*password*
credentials/
secrets/

# Local databases / indexes
*.sqlite
*.sqlite3
*.db
*.db-shm
*.db-wal
index/
indexes/

# Nested staging repos and local VCS artifacts
runtime/hermes/pr-staging/
.git/modules/

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/
